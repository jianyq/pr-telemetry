# PR Telemetry Environment Variables
# Copy this file to .env and add your actual API key
# 
# Usage:
#   1. Copy this file: cp env.example .env
#   2. Edit .env and add your OpenAI API key
#   3. Run: docker-compose up -d

# OpenAI API Configuration
# IMPORTANT: Replace with your actual OpenAI API key for real LLM evaluation
# Or use "mock" for testing without API costs
OPENAI_API_KEY=mock

# Note: Other configuration is in docker-compose.yml
# You only need to set OPENAI_API_KEY in .env file

